<%= form_for(translation) do |f| %>
  <% if translation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(translation.errors.count, "error") %> prohibited this translation from being saved:</h2>

      <ul>
      <% translation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :french_translation %>
    <%= f.text_field :french_translation %>
  </div>

  <div class="field">
    <%= f.label :english_translation %>
    <%= f.text_field :english_translation %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
